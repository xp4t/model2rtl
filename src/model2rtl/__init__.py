"""model2rtl: trained quantized MLP -> portable RTL compiler.

Stage 0 provides the integer arithmetic contract, the quantization flow and
the NumPy integer golden model. No RTL is generated at this stage.
"""

__version__ = "0.1.0"
